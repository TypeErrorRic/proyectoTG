# ==== Comunes (Py 3.8) ====
# Paquete conservador para Jetson + CUDA10.2 (CuPy 10.x) sin avisos de subnormales recientes
numpy==1.21.6
Pillow==9.5.0
tqdm==4.66.4
PyYAML==6.0.1
pandas==1.5.3
json5==0.9.14
scipy==1.7.3
h5py==3.10.0 ; python_version < "3.9"
h5py>=3.10,<4 ; python_version >= "3.9"

# Matplotlib: evita aarch64 (Jetson) para no forzar builds pesados
matplotlib==3.7.5 ; python_version >= "3.8" and platform_machine != "aarch64"
# CuPy 10.6.0 soporta CUDA 10.2 y funciona con NumPy 1.21.x
cupy-cuda102==10.6.0 ; platform_system == "Linux" and platform_machine == "aarch64"

# PyCUDA para TensorRT (inference optimizada en Jetson)
cuda-python ; platform_system == "Linux" and platform_machine == "aarch64"
