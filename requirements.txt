# ==== Compatibles con Python 3.8 ====
numpy==1.24.4
Pillow==9.5.0
tqdm==4.66.4
PyYAML==6.0.1
pandas==1.5.3
matplotlib==3.7.5 ; python_version >= "3.8" and platform_machine != "aarch64"
json5==0.9.14

# --- PyTorch para Jetson (aarch64) ---
# Nota:
# - JetPack 5.x usa Python 3.8. NVIDIA provee ruedas (wheels) específicas.
# - Esta línea instala Torch (rueda oficial de NVIDIA) SOLO en Jetson (aarch64) con Py 3.8.
# - Si tu Jetson usa JetPack 6.x (Python 3.10), usa la línea comentada de ejemplo.

# JetPack 5.x (Python 3.8): recomendado Torch 2.0.0 (nv23.05)
torch @ https://developer.download.nvidia.com/compute/redist/jp/v51/pytorch/torch-2.0.0%2Bnv23.05-cp38-cp38-linux_aarch64.whl ; platform_machine == "aarch64" and python_version == "3.8"

# Ejemplo JetPack 6.x (Python 3.10):
# torch @ https://developer.download.nvidia.com/compute/redist/jp/v60/pytorch/torch-2.3.0%2Bnv24.06-cp310-cp310-linux_aarch64.whl ; platform_machine == "aarch64" and python_version == "3.10"
